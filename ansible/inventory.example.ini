# Only needed for `scripts/vm-install.sh --inventory <file>` or direct ansible-playbook use;
# otherwise VM_HOST / VM_SSH_USER build a one-host inventory for you.
[o11y_vm]
vm-o11y-01 ansible_host=10.0.0.4 ansible_user=azureuser
